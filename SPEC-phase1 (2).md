# SPEC: Полное обновление чекера — Фаза 1

## Контекст

Проект — RKN Checker, форк `klodklodin26-oss/rkn-checker`. Next.js 16 + TypeScript. Сейчас работает в двух режимах: `demo` (фейковые данные без API-ключа) и `ai` (вызов Claude Sonnet 4.5 с HTML главной страницы).

Цель Фазы 1 — превратить базовый AI-чекер в **серьёзный продукт с двумя тарифами** и **глубоким анализом**, который реально (а не только на словах) превосходит большинство конкурентов в этой нише.

Все улучшения делаются **без headless-браузера** (Playwright/Puppeteer). Реальный cookie-аудит и анализ работы трекеров в рантайме — отдельная Фаза 2, делаем потом.

## Финальная архитектура

Два режима вместо трёх. `demo` удаляем полностью.

| Режим | Технология | Стоимость для нас | Скорость | Глубина |
|---|---|---|---|---|
| **Quick** (бесплатно для всех) | Regex по HTML | 0 ₽ | <1 сек | Базовая, 15-17 правил |
| **Pro** (платно/по email-лиду) | Многостраничный fetch + Claude AI + детекторы | ~15-20 ₽ за проверку | 20-40 сек | Глубокая, все 36 правил + чтение политики + детект CMP |

## Этапы реализации

Делай поэтапно. После каждого этапа: `npm run build`, `npm run dev`, проверь что работает, покажи мне результат. Только потом следующий этап.

---

### Этап 1: Quick-режим (регулярки)

Создать `app/lib/basic-check.ts`. Функция `basicCheckHtml(url, html)` возвращает `CheckResult` с режимом `"quick"`.

**Правила для Quick-режима** (используй ID из существующего `app/lib/laws.ts`, не выдумывай новые ID):

1. **`google-analytics`** — регулярка `/google-analytics\.com|googletagmanager\.com|gtag\s*\(|ga\s*\(\s*['"]create['"]/i`
2. **`google-fonts`** — `/fonts\.googleapis\.com|fonts\.gstatic\.com/i`
3. **`facebook-pixel`** — `/connect\.facebook\.net|fbq\s*\(/i` (см. также правило 6 ниже про новый смысл)
4. **`meta-links-text`** (LOW риск) — найден текст "instagram" или "facebook" в HTML И в HTML нет слова "экстремистск" / "запрещена" / "признана экстремистской" в радиусе 500 символов. **Не считать совпадением**: вхождения внутри `class="..."`, `href="..."`, путей к файлам, имён классов. Только видимый текст или текст в `<a>` тегах.
5. **`meta-logo`** (HIGH риск) — найден логотип Instagram или Facebook. Сигналы:
   - Классы иконок: `fa-instagram`, `fa-facebook`, `fa-facebook-f`, `bi-instagram`, `bi-facebook`, `icon-instagram`, `icon-facebook`, `fab fa-instagram`, `fab fa-facebook`
   - Имена файлов: `<img src=".../instagram.svg|png|jpg|webp">`, `.../facebook.svg|png|jpg|webp`, `.../ig.svg`, `.../fb.svg`, `.../ig-icon.*`, `.../fb-icon.*`
   - SVG-атрибуты: `<svg ... aria-label="Instagram">` или `aria-label="Facebook"` (или title с этими словами)
   - CDN-сигнатуры: `fontawesome.com/.../instagram`, аналогично facebook
6. **`meta-ads-pixel`** (HIGH риск) — найден Facebook/Meta Pixel или Conversions API — те же сигнатуры что в правиле `facebook-pixel`. Это считается признаком активной рекламной активности (статья новая с 1.09.2025: реклама в запрещённых соцсетях). Объединить с правилом `facebook-pixel`, если они в `laws.ts` уже не разделены — оставь одно правило с более жёсткой формулировкой про рекламу.

**Дальнейшая нумерация смещается:**

7. **`pd-policy`** — нет ссылки `<a>` с текстом, содержащим "политика конфиденциальности" / "политика обработки" / "обработка персональных" / "privacy"
8. **`cookie-banner`** — в HTML нет пары "cookie"|"куки" + "принять"|"согласен"|"accept" в пределах 300 символов
9. **`lang-headers`** — в `<nav>`, `<header>` и первых 5 `<a>` доля кириллицы <20%
10. **`lang-anglicisms`** — найдено 2+ из списка `["sale", "new", "premium", "contact us", "subscribe", "buy now", "more info", "join us"]` в кнопках/заголовках

**Доп. правила, если соответствующие ID есть в `laws.ts`** (если нет — пропусти, не выдумывай):

11. Яндекс.Метрика — `/mc\.yandex\.ru|ym\s*\(\s*\d+/i`
12. reCAPTCHA Google — `/google\.com\/recaptcha|grecaptcha/i`
13. VK Pixel — `/vk-cdn\.net\/pixel|vk\.com\/js\/api\/openapi\.js/i`
14. Mixed content — на HTTPS-странице есть `<img src="http://...">` или `<script src="http://...">`
15. `pd-cross-border` — добавляется автоматически, если сработали правила #1, #2, #3 или #12 (иностранные сервисы → нужно согласие на трансграничную передачу)

**В Quick-режиме НЕ обрезай HTML до 200 КБ.** Регулярки работают на всём, что скачалось.

Файл `app/lib/types.ts`: в `CheckResult.mode` добавить `"quick"`. Удалить `"demo"` если хочешь чистоту (или оставить — на твой выбор).

**Проверка этапа:** на странице `localhost:3000` выбрать Quick → ввести `https://habr.com` → должно найти Google Analytics, Yandex.Metrika, отсутствие cookie-баннера. На `https://example.com` → ноль или 1 нарушение.

---

### Этап 1.5: Правильный User-Agent и детект SPA-сайтов

**Критично для адекватности.** Текущий код в `app/lib/fetch-site.ts` использует User-Agent `Mozilla/5.0 (compatible; RKN-Checker/1.0; +https://example.com/bot)` — большие сайты (Яндекс, Озон, ВК, Wildberries) видят это и отдают пустую заглушку. А JS-рендеренные сайты в принципе не отдают контент через простой fetch — у них в HTML только пустой `<div id="root">`.

#### 1.5a — Реальный User-Agent

В `app/lib/fetch-site.ts` поменять User-Agent на актуальный браузерный:

```ts
"User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
```

И добавить заголовки, типичные для браузера:
```ts
"Accept": "text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,*/*;q=0.8",
"Accept-Language": "ru-RU,ru;q=0.9,en;q=0.8",
"Accept-Encoding": "gzip, deflate, br",
```

#### 1.5b — Детектор SPA

Создать `app/lib/spa-detector.ts`. Функция возвращает признаки JS-рендеринга:

```ts
export type SpaDetection = {
  isLikelySpa: boolean;
  signals: string[]; // что именно указывает на SPA
  textContentLength: number; // объём видимого текста в HTML
};

export function detectSpa(html: string): SpaDetection {
  const signals: string[] = [];

  // Сигнал 1: совсем мало текстового контента
  const textOnly = html.replace(/<script[\s\S]*?<\/script>/gi, '')
                       .replace(/<style[\s\S]*?<\/style>/gi, '')
                       .replace(/<[^>]+>/g, ' ')
                       .replace(/\s+/g, ' ')
                       .trim();
  const textLength = textOnly.length;

  if (textLength < 500 && html.length > 5000) {
    signals.push(`Очень мало видимого текста (${textLength} символов) при размере HTML ${html.length}`);
  }

  // Сигнал 2: характерные маркеры фреймворков
  if (/__NEXT_DATA__|__NUXT__|window\.__INITIAL_STATE__|ng-version=|data-reactroot|v-app/.test(html)) {
    signals.push('Обнаружены признаки React/Next/Vue/Nuxt/Angular');
  }

  // Сигнал 3: пустые root-контейнеры
  if (/<div\s+id=["'](root|app|__nuxt|__next)["'][^>]*>\s*<\/div>/.test(html)) {
    signals.push('Пустой root-контейнер SPA-фреймворка');
  }

  // Сигнал 4: преобладание скриптов над контентом
  const scriptMatches = html.match(/<script[\s\S]*?<\/script>/gi) || [];
  const totalScriptSize = scriptMatches.reduce((s, m) => s + m.length, 0);
  if (totalScriptSize > textLength * 5 && totalScriptSize > 10000) {
    signals.push('Размер скриптов в разы больше видимого контента');
  }

  return {
    isLikelySpa: signals.length >= 2,
    signals,
    textContentLength: textLength,
  };
}
```

#### 1.5c — Использовать в обоих режимах

В `route.ts` после fetch'а главной страницы:
- Прогнать `detectSpa(html)`
- Если `isLikelySpa === true` — добавить в результат поле `warnings: ["Сайт использует JavaScript-рендеринг, часть проверок может быть неточной. Точная проверка таких сайтов появится в Pro+ режиме."]`
- В UI результата (`page.tsx`) показать это как жёлтую плашку в начале списка нарушений, отдельно от самих нарушений

#### 1.5d — Типы

В `app/lib/types.ts` добавить в `CheckResult`:
```ts
warnings?: string[];
spaDetected?: boolean;
```

**Проверка этапа:** прогнать через Quick и Pro:
- `https://yandex.ru` — должен сработать SPA-детект, в результате должна быть плашка
- `https://example.com` — SPA-детект НЕ срабатывает, плашки нет
- Обычный лендинг — то же самое, без плашки

---

### Этап 1.7: Калибровка `laws.ts` — реальные риски и актуальные штрафы

**Что не так с текущим `laws.ts`:** база правил основана на формулировках законов **без учёта поправок мая 2025 года** в КоАП. Штрафы в некоторых правилах не соответствуют действующей редакции (например, для Google Fonts стоит 60–300 тыс. ₽, а актуальные диапазоны по той же статье — другие). Кроме того, **все нарушения сейчас выглядят одинаково страшно**, хотя на практике реальные риски сильно различаются.

**Что нужно сделать:**

#### 1.7a — Обновить типы

В `app/lib/laws.ts` расширить тип `LawRule`:

```ts
export type LawRule = {
  id: string;
  category: string;
  title: string;
  law: string;
  fineMin: number;
  fineMax: number;
  howToCheck: string;
  // НОВЫЕ ПОЛЯ:
  realRiskLevel: "high" | "medium" | "low";
  enforcementNote?: string; // 1-2 предложения о реальной судебной практике
  lawArticle?: string; // конкретная статья КоАП, например "ч. 8 ст. 13.11 КоАП РФ"
  lastReviewed?: string; // дата последней сверки, формат "2026-05"
};
```

Аналогично в типе `Violation` (если он отдельный в `types.ts`) — добавить те же поля.

#### 1.7b — Актуальные штрафы по основным составам (на май 2026)

Используй эти референсные значения для калибровки. Это **актуальные диапазоны после поправок ФЗ от 30.11.2024 № 420-ФЗ**, вступивших в силу 30 мая 2025 и 1 июля 2025:

| Состав | Статья КоАП | Штраф для юр.лица |
|---|---|---|
| Нарушение локализации ПДн (хранение за рубежом) | ч. 8 ст. 13.11 | 1 000 000 – 6 000 000 ₽ |
| Повторное нарушение локализации в течение года | ч. 9 ст. 13.11 | 6 000 000 – 18 000 000 ₽ |
| Обработка ПДн без согласия / с нарушением правил | ч. 1 ст. 13.11 | 300 000 – 700 000 ₽ |
| Обработка специальных категорий ПДн без согласия | ч. 2 ст. 13.11 | 300 000 – 700 000 ₽ |
| Необеспечение записи, систематизации, хранения с использованием баз в РФ | ч. 8 ст. 13.11 | 1 000 000 – 6 000 000 ₽ |
| Неуведомление РКН о начале обработки | ст. 19.7 (в новой ред.) | 100 000 – 300 000 ₽ |
| Неуведомление РКН об утечке (24 ч.) | ч. 10–11 ст. 13.11 | 1 000 000 – 3 000 000 ₽ |
| Неопубликование политики обработки ПДн | ч. 3 ст. 13.11 | 60 000 – 100 000 ₽ |
| Нарушение прав субъекта ПДн (отказ удалить и т.п.) | ч. 4 ст. 13.11 | 80 000 – 100 000 ₽ |
| Реклама без маркировки «erid» | ст. 14.3 ч. 16 | 200 000 – 500 000 ₽ |
| Реклама с недостоверной информацией («лучший», «№1» без подтверждения) | ст. 14.3 ч. 1 | 100 000 – 500 000 ₽ |
| Реклама медуслуг без предупреждения о противопоказаниях | ст. 14.3.1 | 200 000 – 500 000 ₽ |
| Нарушение требований к гос.языку (англицизмы в маркетинговых материалах) | ст. 28.1 ФЗ-53, ст. 5 КоАП | 40 000 – 100 000 ₽ |
| Использование запрещённых символов экстремистских организаций (Meta-ссылки без пометки) | ст. 13.15 ч. 11 | 4 000 – 50 000 ₽ |

**Если в текущем `laws.ts` значения отличаются от референсных — обнови.** Если для какого-то правила не находишь статью в таблице выше — задай вопрос в чате со мной (Claude), не выдумывай.

#### 1.7c — Калибровка risk-уровней

Используй эти ориентиры для проставления `realRiskLevel`:

**HIGH** (красный, выводить наверху списка):
- Локализация: Google Analytics, Google Tag Manager, Google Forms, иностранные CRM/облака
- Отсутствие cookie-баннера (если есть формы с ПДн)
- Отсутствие политики обработки ПДн
- Отсутствие согласия на обработку ПДн в формах
- Сбор биометрических ПДн без письменного согласия
- Реклама без маркировки «erid»
- **`meta-logo`**: логотип Instagram/Facebook на сайте (ст. 20.3 КоАП — символика экстремистской организации, реальная практика штрафов есть)
- **`meta-ads-pixel`**: Facebook/Meta Pixel + признаки рекламной активности (с 1.09.2025 запрещена реклама в Meta-соцсетях, ст. 14.3 КоАП)
- `enforcementNote`: "РКН и прокуратура ведут активную проверочную работу с 2025 года. Штрафы по этой статье назначаются регулярно."

**MEDIUM** (жёлтый):
- Google Fonts (формально та же логика что GA, но реальной судебной практики в РФ почти нет)
- Google reCAPTCHA, Stripe Elements, иные иностранные виджеты
- Facebook Pixel сам по себе без признаков активной рекламы (Meta — экстремистская, но штраф зависит от характера использования)
- Неуведомление РКН (для маленьких сайтов проверки редки)
- Реклама с недостоверной информацией («лучший», «№1»)
- `enforcementNote`: "Юридический риск реальный, но массовых проверок РКН по этому составу пока не зафиксировано. Рекомендуется устранить как профилактическая мера."

**LOW** (серый):
- Англицизмы в навигации/кнопках
- Заголовки на иностранном языке
- **`meta-links-text`**: текстовое упоминание Instagram/Facebook без пометки "экстремистская организация" (ст. 13.15 ч. 2 КоАП — есть закон, но реальная практика для коммерческих сайтов почти нулевая, штрафы выписывают в основном СМИ)
- Технические недочёты политики (формулировки, оформление)
- `enforcementNote`: "Нарушение формальное, штрафы крайне редки для обычного бизнеса. Имеет смысл устранить для общего качества, но риск проверки минимальный."

#### 1.7c-bis — Разделение правила `meta-links` на три

Если в `laws.ts` есть единое правило `meta-links` — **разделить его** на три отдельных правила:

```ts
{
  id: "meta-links-text",
  category: "Иностранные сервисы",
  title: "Текстовое упоминание Instagram/Facebook без пометки об экстремистской организации",
  law: "ч. 2 ст. 13.15 КоАП РФ",
  fineMin: 4000,
  fineMax: 50000,
  realRiskLevel: "low",
  enforcementNote: "Для обычных коммерческих сайтов реальной практики штрафов почти нет — обычно выносится предупреждение от прокуратуры. Высокий риск только для СМИ и публичных персон с большой аудиторией.",
  howToCheck: "Найти 'instagram' или 'facebook' в тексте, проверить отсутствие пометки рядом",
},
{
  id: "meta-logo",
  category: "Иностранные сервисы",
  title: "Логотип Instagram/Facebook на сайте (символика экстремистской организации)",
  law: "ст. 20.3 КоАП РФ",
  fineMin: 1000,
  fineMax: 50000,
  realRiskLevel: "high",
  enforcementNote: "Демонстрация символики запрещённой организации — реальные штрафы есть в практике 2022–2026 годов. Срочно убрать логотипы из шапки, подвала, кнопок 'Поделиться'.",
  howToCheck: "Найти иконки с классами fa-instagram/fa-facebook, файлы instagram.svg/facebook.svg, SVG с aria-label",
},
{
  id: "meta-ads-pixel",
  category: "Иностранные сервисы",
  title: "Признаки рекламной активности в Meta-соцсетях (Facebook Pixel, Conversions API)",
  law: "ст. 14.3 КоАП РФ (с 1.09.2025) + риск ст. 282.3 УК РФ",
  fineMin: 300000,
  fineMax: 1000000,
  realRiskLevel: "high",
  enforcementNote: "С 1 сентября 2025 года реклама в Meta-соцсетях полностью запрещена. Установленный Facebook Pixel — индикатор активной кампании. Также есть риск уголовной квалификации как финансирование экстремизма (ст. 282.3 УК РФ).",
  howToCheck: "Найти connect.facebook.net, fbq(), события init/track Pixel",
},
```

Старое правило `meta-links` удалить или оставить как deprecated (с пометкой в комментарии).

#### 1.7d — Обновить промпт Claude для Pro-режима

В системном промпте `app/lib/claude.ts` добавить:
- Объяснение, что каждое нарушение должно иметь `realRiskLevel`
- Инструкцию: брать `realRiskLevel` из соответствующего `LawRule` (не выдумывать)
- Если AI находит нарушение, которого нет в `LAWS` — пометить его как `realRiskLevel: "medium"` по умолчанию

#### 1.7e — UI: сортировка и визуальная дифференциация

В `app/page.tsx` (страница результатов):

1. **Сортировка** — нарушения в порядке: HIGH → MEDIUM → LOW. В пределах одного уровня — по убыванию `fineMax`.
2. **Визуальная разметка** — каждое нарушение получает цветовую полоску слева:
   - HIGH: красный (`border-l-4 border-red-500`)
   - MEDIUM: жёлтый (`border-l-4 border-yellow-500`)
   - LOW: серый (`border-l-4 border-gray-300`)
3. **Бейдж уровня риска** — рядом с заголовком нарушения: "Высокий риск штрафа" / "Средний риск" / "Низкий риск"
4. **Блок с enforcementNote** — внутри карточки нарушения, в формате цитаты или примечания: "**Реальная практика:** {enforcementNote}"
5. **Сводная панель сверху** — показать счётчики: "X высоких рисков, Y средних, Z низких"

#### 1.7f — Дисклеймер на странице результатов

Добавить в футер результатов **обязательный дисклеймер** (это важно юридически — чтобы тебя самого не привлекли за "введение в заблуждение"):

> ⚠️ Результаты автоматической проверки носят информационный характер и не являются юридическим заключением. Реальная квалификация нарушений зависит от обстоятельств, статуса оператора ПДн, поданных в Роскомнадзор уведомлений и судебной практики. Для точной оценки рисков обратитесь к юристу.

#### 1.7g — Поле `lastReviewed`

Для каждого правила, которое откалибровал — проставь `lastReviewed: "2026-05"`. Через полгода-год нужно будет пересмотреть (законодательство быстро меняется), и это поле будет напоминать, что данные устарели.

**Проверка этапа:** на странице результатов:
- Видна сводка "X высоких / Y средних / Z низких рисков"
- Нарушения отсортированы правильно (HIGH сверху)
- У каждого правильный цвет полоски
- Виден дисклеймер внизу
- Suммарные штрафы пересчитаны на основе обновлённых `fineMin`/`fineMax`

---

### Этап 1.8: Реквизиты юр.лица и ЗоЗПП

**Контекст:** анализ конкурента (chistyisait.ru) показал, что мы не проверяем **ключевые требования закона о защите прав потребителей** (ЗоЗПП): реквизиты юр.лица, публичную оферту, условия возврата, цены в рублях. Это серьёзный пробел, особенно для коммерческих сайтов и интернет-магазинов, которых проверяет Роспотребнадзор.

Добавляем **5 новых правил**. Часть из них доступна в Quick-режиме (регулярки по HTML главной), часть — в Pro-режиме (требует многостраничный fetch + AI-анализ для определения "коммерческий ли это сайт").

#### 1.8a — Детектор коммерческого сайта

Часть правил применима только если сайт **коммерческий** (продаёт что-то, а не блог/визитка). Создать `app/lib/commercial-detector.ts`:

```ts
export type CommercialSignals = {
  isCommercial: boolean;
  confidence: "high" | "medium" | "low";
  signals: string[];
};

export function detectCommercial(html: string): CommercialSignals {
  const signals: string[] = [];

  // E-commerce платформы
  if (/bitrix|catalog\.action|opencart|tilda.*store|insales|shopify/i.test(html)) {
    signals.push("Обнаружена e-commerce платформа");
  }

  // Корзина, кнопки покупки
  if (/корзина|в корзину|купить|add to cart|оформить заказ|checkout/i.test(html)) {
    signals.push("Элементы корзины или кнопки покупки");
  }

  // Ценники с рублями
  if (/(\d[\d\s]{0,10})\s*(₽|руб|RUB)/.test(html)) {
    signals.push("Ценники в рублях");
  }

  // Каталог товаров
  if (/каталог|товары|product-card|product__|item-card/i.test(html)) {
    signals.push("Каталог или карточки товаров");
  }

  // Оплата/доставка
  if (/способы оплаты|доставка|тарифы доставки|курьер|сдэк|почта россии/i.test(html)) {
    signals.push("Информация об оплате/доставке");
  }

  return {
    isCommercial: signals.length >= 2,
    confidence: signals.length >= 3 ? "high" : signals.length === 2 ? "medium" : "low",
    signals,
  };
}
```

#### 1.8b — Новые правила в `laws.ts`

```ts
{
  id: "requisites-missing",
  category: "Реквизиты юр.лица",
  title: "Не указаны реквизиты юридического лица (ИНН, ОГРН, адрес, наименование)",
  law: "ст. 9 ЗоЗПП, ч. 1 ст. 14.5 КоАП РФ",
  fineMin: 30000,
  fineMax: 40000,
  realRiskLevel: "medium",
  enforcementNote: "Роспотребнадзор регулярно проверяет коммерческие сайты, особенно интернет-магазины. Штрафы выписываются при плановых и внеплановых проверках.",
  howToCheck: "Поиск ИНН (10/12 цифр), ОГРН (13/15 цифр), КПП (9 цифр), названия юр.лица в footer или на странице 'Контакты'/'Реквизиты'",
  lastReviewed: "2026-05",
},
{
  id: "offer-missing",
  category: "Документы продавца",
  title: "Публичная оферта или условия использования не найдены",
  law: "ст. 435–437 ГК РФ, ст. 26.1 ЗоЗПП",
  fineMin: 10000,
  fineMax: 30000,
  realRiskLevel: "medium",
  enforcementNote: "Для интернет-магазинов отсутствие оферты — повод для штрафа от Роспотребнадзора. Применяется при наличии жалоб покупателей.",
  howToCheck: "Ссылка с текстом 'оферта', 'договор-оферта', 'условия продажи', 'пользовательское соглашение' в footer (только для коммерческих сайтов)",
  lastReviewed: "2026-05",
},
{
  id: "return-policy-missing",
  category: "Документы продавца",
  title: "Условия возврата товара не указаны (для интернет-магазинов)",
  law: "ст. 26.1 ЗоЗПП",
  fineMin: 10000,
  fineMax: 30000,
  realRiskLevel: "medium",
  enforcementNote: "Роспотребнадзор активно проверяет интернет-магазины на наличие правил возврата. При продаже дистанционным способом 7-дневный срок возврата обязателен.",
  howToCheck: "Ссылка с текстом 'возврат', 'обмен', 'гарантия', 'возврат товара' в footer (только для коммерческих сайтов)",
  lastReviewed: "2026-05",
},
{
  id: "contacts-missing",
  category: "Реквизиты юр.лица",
  title: "Контактная информация (телефон или email) не указана",
  law: "ст. 8 ЗоЗПП, ч. 1 ст. 14.8 КоАП РФ",
  fineMin: 5000,
  fineMax: 10000,
  realRiskLevel: "low",
  enforcementNote: "Минимальные требования по контактам обычно соблюдаются. Штрафы по этому пункту крайне редки.",
  howToCheck: "Поиск номера телефона (формат +7 или 8) или email-адреса в footer или на странице 'Контакты'",
  lastReviewed: "2026-05",
},
{
  id: "prices-currency",
  category: "Цены и оферта",
  title: "Цены указаны не в рублях",
  law: "ст. 10 ЗоЗПП",
  fineMin: 5000,
  fineMax: 10000,
  realRiskLevel: "low",
  enforcementNote: "Цены обязательно должны быть в рублях. Дополнительно можно указывать в у.е., $, €. Нарушение только когда рубли отсутствуют совсем.",
  howToCheck: "Найти ценники, проверить наличие ₽/руб/RUB рядом с числами",
  lastReviewed: "2026-05",
}
```

#### 1.8c — Детекторы для Quick-режима

В `app/lib/basic-check.ts` добавить:

```ts
// ИНН: 10 цифр (юр.лицо) или 12 цифр (ИП)
const innRegex = /\bИНН[:\s]*(\d{10}|\d{12})\b/i;

// ОГРН: 13 цифр (юр.лицо) или 15 цифр (ОГРНИП)
const ogrnRegex = /\bОГРН(?:ИП)?[:\s]*(\d{13}|\d{15})\b/i;

// КПП: 9 цифр (только юр.лица, у ИП нет)
const kppRegex = /\bКПП[:\s]*(\d{9})\b/i;

// Юр.адрес: индекс + слова "город"/"г." или явное "юридический адрес"
const legalAddressRegex = /юридический адрес|юр\.\s?адрес|\b\d{6}[,\s]+(?:г\.|город|обл\.|область)/i;

// Название юр.лица
const legalNameRegex = /\b(ООО|АО|ПАО|ИП|ОАО|ЗАО)\s+["«][^"»]+["»]/;

// Телефон в РФ
const phoneRegex = /(?:\+7|8|7)[\s\-()]*\d{3}[\s\-()]*\d{3}[\s\-()]*\d{2}[\s\-()]*\d{2}/;

// Email
const emailRegex = /[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}/i;

// Цены в рублях
const rubPriceRegex = /(\d[\d\s]{0,10})\s*(₽|руб\.?|RUB)/;

// Цены НЕ в рублях (только)
const foreignCurrencyRegex = /(\d[\d\s]{0,10})\s*(\$|€|USD|EUR|у\.\s?е\.)/;
```

**Логика проверок (для Quick):**

```ts
// 1. Реквизиты юр.лица — проверяем наличие ИНН, ОГРН, названия
const hasInn = innRegex.test(html);
const hasOgrn = ogrnRegex.test(html);
const hasLegalName = legalNameRegex.test(html);

if (!hasInn || !hasOgrn || !hasLegalName) {
  const missing = [
    !hasInn && "ИНН",
    !hasOgrn && "ОГРН",
    !hasLegalName && "название юр.лица"
  ].filter(Boolean).join(", ");
  add("requisites-missing", `Не найдено: ${missing}`, "medium");
}

// 2. Контакты — телефон или email хотя бы один
if (!phoneRegex.test(html) && !emailRegex.test(html)) {
  add("contacts-missing", "Не найден ни телефон, ни email на главной странице", "low");
}

// 3. Цены в рублях — только если есть признаки коммерческого сайта
const commercial = detectCommercial(html);
if (commercial.isCommercial) {
  // Если есть цены в иностранной валюте и НЕТ цен в рублях
  if (foreignCurrencyRegex.test(html) && !rubPriceRegex.test(html)) {
    add("prices-currency", "Найдены цены в иностранной валюте без рублёвого эквивалента", "low");
  }

  // 4. Оферта (только для коммерческих)
  if (!/<a[^>]*>[^<]*(?:оферт|условия использования|пользовательское соглашение|публичный договор)/i.test(html)) {
    add("offer-missing", "На сайте не найдена ссылка на оферту/условия продажи", "medium");
  }

  // 5. Возврат (только если есть признаки магазина — корзина и т.п.)
  if (commercial.signals.some(s => s.includes("корзин") || s.includes("e-commerce"))) {
    if (!/<a[^>]*>[^<]*(?:возврат|обмен|гарантия товар)/i.test(html)) {
      add("return-policy-missing", "Не найдена страница с условиями возврата товара", "medium");
    }
  }
}
```

#### 1.8d — Расширить многостраничный fetch (Этап 2)

В `fetch-pages.ts` (см. Этап 2) добавить **дополнительные страницы для скачивания**:

- "Контакты", "Реквизиты", "О компании", "About"
- "Оферта", "Договор-оферта", "Условия использования", "Публичный договор"
- "Возврат", "Гарантия", "Возврат товара", "Обмен"
- "Доставка", "Оплата"

В `SitePages` добавить поля:
```ts
type SitePages = {
  // ... существующие
  offer?: { url: string; content: string; status: number };
  returnPolicy?: { url: string; content: string; status: number };
  payment?: { url: string; content: string; status: number };
};
```

В Pro-режиме после скачивания этих страниц повторно прогнать регулярки из 1.8c **на всех страницах**, не только на главной — реквизиты часто бывают только на странице "Контакты".

#### 1.8e — Инструкции для AI-промпта (Pro)

В `claude.ts` добавить раздел в системный промпт:

> Также проверь требования закона о защите прав потребителей (ЗоЗПП):
>
> 1. Найди на сайте (главная + страница контактов + реквизиты): ИНН, ОГРН/ОГРНИП, КПП, юридический адрес, полное наименование организации
> 2. Для коммерческих сайтов (определи по наличию корзины, цен, кнопок "купить"):
>    - Должна быть публичная оферта или пользовательское соглашение
>    - Для интернет-магазинов — обязательны условия возврата по ст. 26.1 ЗоЗПП
>    - Цены должны быть в рублях
> 3. Если сайт некоммерческий (блог, визитка, новостной портал) — нарушения по ЗоЗПП не применяются

#### 1.8f — Дифференциация для некоммерческих сайтов

Если `detectCommercial(html).isCommercial === false`, то правила `offer-missing`, `return-policy-missing`, `prices-currency` **не применяются**. Это критично: блог или визитка не должна получать "нарушение за отсутствие оферты".

Правила `requisites-missing` и `contacts-missing` применяются ко всем коммерческим организациям (то есть к любому сайту юр.лица или ИП, даже если он не продаёт онлайн).

**Проверка этапа:** на тестовых сайтах:
- Интернет-магазин (например `https://ozon.ru` или любой малый магазин) — Quick должен найти все или почти все ЗоЗПП-нарушения
- Сайт-визитка с реквизитами — `requisites-missing` НЕ срабатывает
- Личный блог — никакие ЗоЗПП-правила не срабатывают (потому что `isCommercial === false`)

---

### Этап 2: Многостраничный fetch для Pro

Создать `app/lib/fetch-pages.ts`. Функция `fetchSitePages(url)`:

1. Скачивает главную страницу (как раньше)
2. Парсит HTML, находит ссылки `<a>` с текстом, содержащим:
   - **Политика**: "политика конфиденциальности", "политика обработки", "обработка персональных", "privacy"
   - **Условия**: "пользовательское соглашение", "условия использования", "terms"
   - **Контакты/реквизиты**: "контакты", "о компании", "реквизиты"
3. Резолвит относительные ссылки до абсолютных
4. Скачивает каждую из найденных страниц (максимум 5 штук)
5. Если ссылка на политику ведёт на `.pdf` — скачивает PDF, извлекает текст через библиотеку `pdf-parse` (`npm install pdf-parse`)
6. Возвращает объект:

```ts
type SitePages = {
  homepage: { url: string; html: string; status: number };
  policy?: { url: string; content: string; isPdf: boolean; status: number };
  terms?: { url: string; content: string; isPdf: boolean; status: number };
  contacts?: { url: string; content: string; status: number };
  extraPages: { url: string; content: string; status: number }[];
};
```

**Таймауты и лимиты:**
- Один запрос — максимум 10 секунд
- Общий лимит — 30 секунд на все страницы
- Если страница политики отдала 404 / 500 / пустоту — это **отдельное нарушение** (rule `pd-policy-link` или `pd-policy`)
- Размер каждой страницы — до 500 КБ (для Pro можно больше, потому что нам важно глубокое содержимое)

**Проверка этапа:** написать тестовый вызов `fetchSitePages('https://habr.com')`, в логи распечатать сколько страниц скачано и какого размера. Убедиться, что находит политику и пользовательское соглашение.

---

### Этап 3: Детектор CMP (Consent Management Platforms)

Создать `app/lib/cmp-detector.ts`. Содержит список известных CMP-провайдеров и их сигнатур:

```ts
export type CmpInfo = { name: string; signatures: RegExp[] };

export const KNOWN_CMPS: CmpInfo[] = [
  { name: "Cookiebot", signatures: [/consent\.cookiebot\.com/i, /cookiebot\.js/i] },
  { name: "OneTrust", signatures: [/cdn\.cookielaw\.org/i, /onetrust/i, /optanonconsent/i] },
  { name: "CookieYes", signatures: [/cookie-cdn\.cookieyes\.com/i, /cookieyes\.com/i] },
  { name: "Termly", signatures: [/termly\.io/i, /app\.termly\.io/i] },
  { name: "Iubenda", signatures: [/iubenda\.com/i, /cdn\.iubenda\.com/i] },
  { name: "Osano", signatures: [/cmp\.osano\.com/i] },
  { name: "TrustArc", signatures: [/trustarc\.com/i, /truste\.com/i] },
  { name: "Quantcast", signatures: [/quantcast/i, /quantserve\.com/i] },
  { name: "Sourcepoint", signatures: [/sourcepoint\.com/i, /sp-prod/i] },
  { name: "Didomi", signatures: [/didomi\.io/i] },
  { name: "Usercentrics", signatures: [/usercentrics\.com/i, /usercentrics\.eu/i] },
  { name: "Klaro", signatures: [/klaro\.kiprotect/i, /klaro\.js/i] },
  { name: "CookieScript", signatures: [/cookie-script\.com/i] },
  { name: "Tarte au Citron", signatures: [/tarteaucitron\.js/i] },
  { name: "Civic Cookie Control", signatures: [/cookiecontrol/i] },
  { name: "CookieConsent (Insites)", signatures: [/cookieconsent\.insites/i] },
  { name: "Axeptio", signatures: [/axept\.io/i] },
  { name: "Complianz", signatures: [/complianz\.gdpr/i, /cmplz-/i] },
  { name: "GDPR Cookie Compliance", signatures: [/moove_gdpr/i] },
  { name: "CookiePro", signatures: [/cookiepro/i] },
  // Российские
  { name: "Cookieinformation", signatures: [/cookieinformation\.com/i] },
  { name: "Cookiescan", signatures: [/cookiescan\.ru/i] },
];

export function detectCmp(html: string): { detected: boolean; name?: string } {
  for (const cmp of KNOWN_CMPS) {
    for (const sig of cmp.signatures) {
      if (sig.test(html)) {
        return { detected: true, name: cmp.name };
      }
    }
  }
  return { detected: false };
}
```

В Pro-режиме при анализе:
- Если CMP детектирован → отдельное поле в результате `cmpDetected: { name: "Cookiebot" }`
- Если CMP не детектирован, но в HTML есть похожие на баннер слова → пометить как "вероятно есть кастомный баннер, не из списка известных CMP"
- Если ничего не найдено → нарушение `cookie-banner` усиливается

**Проверка этапа:** проверь известный сайт с Cookiebot (например, поищи через Google `site:example.com cookiebot`) — должен детектировать.

---

### Этап 4: Парсинг чекбоксов и форм

Создать `app/lib/form-analyzer.ts`. Используя простой HTML-парсер (можно `node-html-parser`, `npm install node-html-parser`) или регулярки:

1. Найти все `<form>` в HTML
2. Для каждой формы определить:
   - Какие поля собирает: `email`, `tel`, `name`, текстовые с подходящими атрибутами `placeholder`/`name`
   - Если есть `<input type="email">` или `<input type="tel">` или поле с `name` содержащим "phone"/"email" → форма **собирает ПДн**
3. Для каждой формы, собирающей ПДн:
   - **Есть ли чекбокс согласия?** — `<input type="checkbox">` внутри `<form>` или рядом
   - **Если чекбокс есть — он предзаполнен?** — атрибут `checked` или `checked="checked"` → это **нарушение**, "предзаполненные галочки запрещены ст. 9 ФЗ-152"
   - **Есть ли ссылка на политику от формы?** — внутри `<form>` или в его метке `<label>` должна быть `<a href>` со словами "политика"/"privacy"

Возвращает:
```ts
type FormAnalysis = {
  totalForms: number;
  pdFormsCount: number; // сколько форм собирают ПДн
  formsWithoutConsent: number;
  formsWithPreCheckedConsent: number; // нарушение!
  formsWithoutPolicyLink: number;
};
```

В Pro-режиме результат вливается в нарушения:
- `formsWithPreCheckedConsent > 0` → нарушение со ссылкой на ст. 9 ФЗ-152, штраф 30 000–150 000 ₽
- `formsWithoutPolicyLink > 0` → усиление нарушения `pd-consent`
- `formsWithoutConsent > 0` → нарушение `pd-consent`

**Проверка этапа:** создай локальный HTML-файл с тремя формами (одна правильная, одна без чекбокса, одна с `checked="checked"`) и прогони через analyzer. Должны быть корректные результаты.

---

### Этап 5: PDF-парсер политики (опционально, если ссылка ведёт на PDF)

В `fetch-pages.ts` (Этап 2) уже есть логика "если PDF — распарсить". Здесь реализовать:

```ts
import pdfParse from 'pdf-parse';

async function extractPdfText(buffer: Buffer): Promise<string> {
  const data = await pdfParse(buffer);
  return data.text;
}
```

Установить: `npm install pdf-parse`. Типы: `npm install -D @types/pdf-parse` если есть.

Если парсинг падает (битый PDF, защищённый паролем) — поймать ошибку и зафиксировать как нарушение "Политика недоступна в машиночитаемом виде".

---

### Этап 6: Обновлённый Claude-промпт для Pro-режима

Файл `app/lib/claude.ts`. Системный промпт переписать так, чтобы:

1. Объяснить Claude, что он получает **многостраничный контент**: главная + политика + условия + контакты
2. Дать ему уже посчитанные результаты: список найденных CMP, результаты form-analyzer, мета-информацию о страницах
3. Поставить новые задачи:
   - Проанализировать **содержимое политики** на наличие обязательных пунктов 152-ФЗ:
     - Цели обработки ПДн
     - Перечень обрабатываемых данных
     - Сроки хранения
     - Правовые основания
     - Контакты оператора
     - Права субъектов
   - Если какого-то пункта нет — отдельное нарушение
   - Сопоставить **связь форм с политикой**: указано ли в политике, какие именно формы собирают какие данные
   - Найти на странице **контактов** реквизиты юр.лица (ИНН, ОГРН, наименование), без них — нарушение
   - Найти на главной **маркировку рекламы** (`erid=`), если есть рекламные блоки
   - Найти **превосходные степени** "лучший", "№1", "единственный" в маркетинговом тексте

Промпт должен возвращать строго **JSON-результат** в существующем формате `CheckResult`, чтобы не ломать UI.

Структура входных данных для Claude:
```
URL: <url>
Главная страница (HTML, первые 200 КБ): <html>
Политика конфиденциальности (текст): <содержимое или "не найдена">
Пользовательское соглашение (текст): <содержимое или "не найдено">
Страница контактов (текст): <содержимое или "не найдена">
Найденные CMP: Cookiebot / OneTrust / нет
Анализ форм: всего форм X, собирают ПДн Y, без согласия Z, предзаполненных W
```

---

### Этап 7: Обновить роут `app/api/check/route.ts`

Принимать в теле запроса:
```ts
{ url: string, mode: "quick" | "pro" }
```

Маршрутизация:
- `mode === "quick"` → `fetchSiteHtml(url)` без обрезки → `basicCheckHtml(url, html)` → return result
- `mode === "pro"` → `fetchSitePages(url)` (этап 2) → `detectCmp` (этап 3) → `analyzeFormForms` (этап 4) → `analyzeWithClaude(...)` (обновлённый этап 6) → return result
- Если `mode === "pro"` и нет `ANTHROPIC_API_KEY` → 503 "AI-режим временно недоступен, используйте Quick"

---

### Этап 8: Удалить demo-режим

Полностью удалить `app/lib/demo.ts` и все ссылки на него в коде. Перепроверить `route.ts`, `page.tsx`, `types.ts`. Если где-то осталась логика `if (!apiKey) returnDemo()` — заменить на возврат ошибки или на принудительный Quick-режим.

---

### Этап 9: Обновить главную страницу `app/page.tsx`

1. **Селектор режима** на форме проверки:
   - Радио-кнопки или toggle: "Быстрая проверка (бесплатно)" / "Глубокий аудит (Pro)"
   - По умолчанию — Quick
   - Под Pro показать "от 299 ₽" (точную цену потом, пока без оплаты)
   - При выборе Pro — пока сделать **бесплатным для теста**, без модалки оплаты. Когда подключим платежи — добавим. На данном этапе важнее, что технически Pro работает.

2. **Карточки преимуществ** (те, которые сейчас списаны у конкурента) — переписать **под то, что мы реально делаем**:

   **Политика конфиденциальности (Pro):**
   - Поиск ссылки на политику в HTML
   - Открытие политики, проверка доступности
   - Парсинг PDF-политики, если она в виде PDF
   - Анализ содержимого: цели обработки, перечень данных, сроки, основания, контакты, права субъектов
   - Проверка обновлённости (есть ли дата редакции)

   **Формы и согласия (Pro):**
   - Поиск всех форм на странице
   - Определение, какие формы собирают ПДн (email, телефон, имя)
   - Проверка наличия чекбоксов согласия
   - **Детект предзаполненных галочек (нарушение ст. 9 ФЗ-152)**
   - Связка форм с политикой конфиденциальности

   **Cookies и трекеры (Pro):**
   - Детект 20+ известных CMP-платформ (Cookiebot, OneTrust, CookieYes, Termly, Iubenda и др.)
   - Поиск иностранных трекеров: Google Analytics, Facebook Pixel, Google Fonts, reCAPTCHA
   - Российские трекеры: Яндекс.Метрика, VK Pixel
   - Анализ наличия кастомного cookie-баннера (если CMP не из списка)
   - *Пометить плашкой "Скоро": проверка cookies до клика в реальном браузере — это Фаза 2*

3. **Бейдж "Quick" / "Pro"** на результатах проверки — чтобы пользователь видел каким режимом проверяли.

4. **CTA после Quick** — кнопка "Нашли больше нарушений? Запустить глубокий Pro-аудит за 299 ₽" (пока без оплаты, просто переключатель режима).

---

### Этап 10: Тесты на трёх сайтах

После реализации всех этапов прогнать оба режима на:
- `https://example.com` — должно быть мало нарушений (минималистичный сайт)
- `https://habr.com` — должно найти Google Analytics, Yandex Metrika, какой-то CMP или его отсутствие
- `https://ozon.ru` — крупный российский сайт, должно найти много, включая формы и трекеры

Распечатать результат каждого режима, убедиться что Quick быстрый (<1 сек), Pro работает (20-40 сек), и результаты различаются (Pro находит больше).

---

## Критерии готовности

- [ ] `basic-check.ts` реализован, Quick-режим работает за <1 сек
- [ ] User-Agent в `fetch-site.ts` заменён на реальный браузерный + добавлены Accept-заголовки
- [ ] `spa-detector.ts` создан, в обоих режимах добавляется предупреждение для SPA-сайтов
- [ ] `laws.ts` откалиброван: добавлены `realRiskLevel`, `enforcementNote`, обновлены штрафы по майским поправкам 2025
- [ ] `meta-links` разделено на `meta-links-text`, `meta-logo`, `meta-ads-pixel` с разными уровнями риска
- [ ] Добавлены 5 правил по ЗоЗПП: `requisites-missing`, `offer-missing`, `return-policy-missing`, `contacts-missing`, `prices-currency`
- [ ] `commercial-detector.ts` создан, ЗоЗПП-правила применяются только к коммерческим сайтам
- [ ] На странице результатов: сортировка по уровню риска, цветовые полоски, бейджи, сводка, дисклеймер
- [ ] `fetch-pages.ts` скачивает главную + политику + соглашение + контакты + оферту + возврат
- [ ] `cmp-detector.ts` с 20+ платформами в списке
- [ ] `form-analyzer.ts` парсит формы и находит предзаполненные галочки
- [ ] PDF-парсер работает (если политика в PDF)
- [ ] Claude-промпт переписан, получает многостраничный контент
- [ ] `route.ts` маршрутизирует по `mode`
- [ ] `demo.ts` удалён
- [ ] `page.tsx` показывает выбор режима + честные карточки
- [ ] `npm run build` проходит без ошибок и warnings
- [ ] Pro-режим находит больше нарушений чем Quick на одном и том же сайте

## Деплой после реализации

```powershell
git add .
git commit -m "Phase 1: Quick mode + enhanced Pro mode with multi-page fetch, CMP detection, form analysis"
git push origin main
.\deploy.ps1
```

## Что НЕ делаем в этой задаче

- Платёжный шлюз (ЮKassa / Robokassa) — отдельная задача
- Email-регистрация и личный кабинет — отдельная задача
- Headless-браузер (Playwright) — Фаза 2
- Реальный cookie-аудит (что ставится до клика) — Фаза 2
- Многостраничный crawl всего сайта (а не только 3-4 ключевых) — Фаза 3
- PDF-отчёт по результатам — позже
- Rate limiting и защита от атак — отдельная задача перед публичным запуском

## Если что-то непонятно

Спрашивай меня (Claude в чате) или пользователя. Не выдумывай новые ID правил — используй только те, что в `app/lib/laws.ts`. Если для какой-то проверки в `laws.ts` нет подходящего ID — добавь новое правило в `laws.ts`, согласовав с пользователем (через вопрос в чате Claude Code).

Поехали с Этапа 1.
